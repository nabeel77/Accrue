/**
 * Everything that talks to the chain: the kit RPC client, the generated program client,
 * the Kamino adapter, the Jupiter adapter, the Scope reader, the transaction assembler
 * and the simulator. The adapters land in Phase 3.
 */
export * from './transactionVersion.js';
