/**
 * Pure logic with no network, no database and no Solana imports: position maths,
 * strategy bounds, caps, validation schemas, the position state machine, and the
 * terms and risk acknowledgement texts. Filled in from Phase 1 onwards.
 */
export {};
